const mainBlockButton = document.querySelector(".main__left-block-button");
mainBlockButton.addEventListener("click", () => {
  confirm("Send your message");
});

// const headerLeftBlockItem = document.querySelector(".header__left-block-item");
// headerLeftBlockItem.addEventListener("click", function () {
//   this.classList.add("active");
// });
// _________________________ page 5 __________________________________
const item1Arrow = document.querySelector(".item1__arrow");
item1Arrow.addEventListener("click", () => {
  console.log("item1__arrow");
});

const item2Arrow = document.querySelector(".item2__arrow");
item2Arrow.addEventListener("click", () => {
  console.log("item2__arrow");
});
const item3Arrow = document.querySelector(".item3__arrow");
item3Arrow.addEventListener("click", () => {
  console.log("item3__arrow");
});
const item4Arrow = document.querySelector(".item4__arrow");
item4Arrow.addEventListener("click", () => {
  console.log("item4__arrow");
});
const item5Arrow = document.querySelector(".item5__arrow");
item5Arrow.addEventListener("click", () => {
  console.log("item5__arrow");
});
// _______________________________________________________________________________

// ---------------------------  PAGE 6 -------------------------------------------

function handleFormSubmit(event) {
  event.preventDefault();
  console.log("Send");
  getFormInfo(onlineForm);
}

function getFormInfo(userInfo) {
  // console.log(userInfo.elements);
  const { elements } = userInfo;

  // _______ 3 ____________-

  // return new FormData(userInfo);

  // _________  2  not work______________
  // const data = new FormData();
  // Array.from(elements)
  //   .filter((item) => !!item.name)
  //   .forEach((element) => {
  //     const { name, type } = element;

  //     data.append(name, value);
  //     return data;
  //   });

  // ___________ Page 6 _________________
  const data = Array.from(elements)
    .filter((item) => !!item.name)
    .map((element) => {
      const { name, value } = element;

      return { name, value };
    });

  console.log(data);
  console.log(data.element, data.elements);
  // alert(
  //   `Name: ${data.element}, Email: ${data.element}, Context: ${data.element}, Phone number: ${data.element}`
  // );
}

const onlineForm = document.querySelector(".form__list");
onlineForm.addEventListener("submit", handleFormSubmit);

// ___________________ PAge 7 ___________________
// let swiper_setting = {
//   spaceBetween: 100,
//   pagination: {
//     el: ".swiper-pagination",
//   },
// };

// var swiper_nodes = document.querySelectorAll(".swiper");

/* loop throw */
// [].forEach.call(swiper_nodes, function (swiper_node) {
//   // do whatever
//   let swiper = new Swiper(swiper_node, swiper_setting);
// });

const swiperSetting = {
  // spaceAround: 20,
  slidesPerView: 6,
  spaceBetween: 20,
  centeredSlides: true,
  pagination: {
    el: ".swiper-pagination",
    clickable: true,
  },
};

const swiperList = document.querySelectorAll(".swiper");

[].forEach.call(swiperList, function (swiperItem) {
  const newSwiper = new Swiper(swiperItem, swiperSetting);
});
